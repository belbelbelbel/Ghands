/**
 * Stack screen for Request Service - opened when user taps the + button.
 * Renders the same categories UI without the tab bar.
 */
import CategoryPage from './(tabs)/categories';
export default CategoryPage;
